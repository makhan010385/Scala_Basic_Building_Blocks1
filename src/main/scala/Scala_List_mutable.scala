import scala.collection.mutable.ListBuffer

object Scala_List_mutable {
  def main(args: Array[String]): Unit = {
//    var name = new ListBuffer[datatype]()  // empty buffer is created

    val name = new ListBuffer[String]()
    name+="mk"
    print(name)

  }
}
